{
  "title": "CPU Usage",
  "panels": [
    {
      "type": "graph",
      "title": "CPU Usage (per Pod)",
      "targets": [
        {
          "expr": "sum(rate(container_cpu_usage_seconds_total{container!=\"\"}[5m])) by (pod)",
          "legendFormat": "{{pod}}",
          "refId": "A"
        }
      ]
    }
  ]
}
